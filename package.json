{
  "dependencies": {
    "http-server": "^14.1.1",
    "node-sass": "^7.0.1"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  },
  "scripts": {
    "sass": "node-sass sass/ -o css/ --recursive --watch",
    "server": "npx http-server -p 8080",
    "dev": "npm-run-all --parallel sass server"
  }
}
